# Core: install a CUDA-matched torch FIRST on the Vast box, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# then: pip install -r requirements.txt
torch>=2.3
transformers>=4.45
xgrammar>=0.1.31
accelerate>=0.34
datasets>=3.0
huggingface_hub>=0.25
numpy>=1.26
scipy>=1.11
pandas>=2.1
pyarrow>=15
matplotlib>=3.8
pyyaml>=6.0
tqdm>=4.66
